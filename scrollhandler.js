export const useScrollHandler = (headerSelector) => {
    const header = document.querySelector(headerSelector);

    // document.addEventListener('scroll', ({target}) => {
    //     console.log(target);
    // });
}